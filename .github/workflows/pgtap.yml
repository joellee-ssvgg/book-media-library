name: pgtap

on:
  pull_request:
    paths:
      - "supabase/**"
      - "test/rls/**"
      - ".github/workflows/pgtap.yml"

jobs:
  pgtap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "pgTAP tests are required before Task 01 can be considered complete."
          exit 1
